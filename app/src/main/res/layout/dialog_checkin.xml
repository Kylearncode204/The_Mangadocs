<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_dialog_checkin"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Tiêu đề -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Điểm Danh Nhận Thưởng"
        android:textSize="20sp"
        android:textColor="@color/Blue"
        android:textStyle="bold"
        android:gravity="center"
        android:paddingBottom="8dp"/>

    <!-- GridView hiển thị các ngày điểm danh -->
    <GridLayout
        android:id="@+id/gridCheckin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:columnCount="7"
        android:rowCount="2"
        android:padding="8dp"
        android:layout_marginBottom="16dp">

        <!-- Các ngày sẽ được thêm động từ code Java -->
    </GridLayout>

    <!-- Nút nhận thưởng -->
    <Button
        android:id="@+id/btnCheckin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nhận 10 Xu"
        android:textSize="16sp"
        android:backgroundTint="@color/Blue"
        android:textColor="@android:color/white"
        android:padding="10dp"
        android:layout_marginTop="8dp"/>

    <!-- Hiển thị số xu -->
    <TextView
        android:id="@+id/tvCoins"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Bạn có: 0 xu"
        android:textSize="16sp"
        android:textColor="@color/Blue"
        android:layout_marginTop="10dp"/>
</LinearLayout>
